<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Mac entitlements for Delta. These are the minimum needed for an Electron
  app under hardened runtime to launch + notarize.

  - allow-jit + allow-unsigned-executable-memory: V8 needs JIT pages
  - allow-dyld-environment-variables: Electron's framework loader uses these
  - disable-library-validation: Electron loads its own native modules from the
    .app bundle that aren't signed by Apple
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
